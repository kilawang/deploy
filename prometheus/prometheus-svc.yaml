apiVersion: v1
kind: Service
metadata:
  name: prometheus-svc
  namespace: default
  labels:
     app: prometheus
spec:
  type: ClusterIP
  ports:
  - port: 9090
    protocol: TCP
    targetPort: 9090
  selector:
    app: prometheus
